{
  "name": "wasmwine-monorepo",
  "private": true,
  "workspaces": [ "web" ],
  "scripts": {
    "build:core": "cd core && cargo build --target wasm32-unknown-unknown --release && cp core/target/wasm32-unknown-unknown/release/wasmwine_core.wasm web/public/",
    "build:web": "cd web && npm run build",
    "build": "npm run build:web"
  }
}
